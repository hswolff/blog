// It's spooky time, Ghost time.

var ghost = require('ghost'),
    path  = require('path');

// Once we can pass config file in as
// start up option we'll use htis.
// ghost({
//   config: path.join(__dirname, 'config.js')
// });

ghost();